#ifndef FORTUNA_BUILD_H
#define FORTUNA_BUILD_H

int fortuna_build_project_incremental(const int parallel_build, 
                                      const int incremental_build_override, 
                                      const int lib_only, 
                                      const int run_flag);

#endif // FORTUNA_BUILD_H
